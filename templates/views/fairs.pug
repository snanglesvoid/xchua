extends ../layouts/default

block intro
  .page-header.container
    h2 Fairs

block content
  .page.container
    if fairs.length
      ul.fairs-list
        each fair in fairs
          a(href= "/fair/" + fair.slug)= fair.title
    else
      h5 No Fairs found